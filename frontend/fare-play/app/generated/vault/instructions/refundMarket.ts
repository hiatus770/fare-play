/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getArrayDecoder,
  getArrayEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import { VAULT_PROGRAM_ADDRESS } from "../programs";
import {
  expectSome,
  getAccountMetaFactory,
  type ResolvedAccount,
} from "../shared";
import {
  getRefundDecoder,
  getRefundEncoder,
  type Refund,
  type RefundArgs,
} from "../types";

export const REFUND_MARKET_DISCRIMINATOR = new Uint8Array([
  236, 178, 184, 162, 130, 179, 37, 35,
]);

export function getRefundMarketDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    REFUND_MARKET_DISCRIMINATOR,
  );
}

export type RefundMarketInstruction<
  TProgram extends string = typeof VAULT_PROGRAM_ADDRESS,
  TAccountAdmin extends string | AccountMeta<string> = string,
  TAccountMarketVault extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAdmin extends string
        ? WritableSignerAccount<TAccountAdmin> &
            AccountSignerMeta<TAccountAdmin>
        : TAccountAdmin,
      TAccountMarketVault extends string
        ? WritableAccount<TAccountMarketVault>
        : TAccountMarketVault,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type RefundMarketInstructionData = {
  discriminator: ReadonlyUint8Array;
  marketId: string;
  refunds: Array<Refund>;
};

export type RefundMarketInstructionDataArgs = {
  marketId: string;
  refunds: Array<RefundArgs>;
};

export function getRefundMarketInstructionDataEncoder(): Encoder<RefundMarketInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["marketId", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
      ["refunds", getArrayEncoder(getRefundEncoder())],
    ]),
    (value) => ({ ...value, discriminator: REFUND_MARKET_DISCRIMINATOR }),
  );
}

export function getRefundMarketInstructionDataDecoder(): Decoder<RefundMarketInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["marketId", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ["refunds", getArrayDecoder(getRefundDecoder())],
  ]);
}

export function getRefundMarketInstructionDataCodec(): Codec<
  RefundMarketInstructionDataArgs,
  RefundMarketInstructionData
> {
  return combineCodec(
    getRefundMarketInstructionDataEncoder(),
    getRefundMarketInstructionDataDecoder(),
  );
}

export type RefundMarketAsyncInput<
  TAccountAdmin extends string = string,
  TAccountMarketVault extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  admin: TransactionSigner<TAccountAdmin>;
  marketVault?: Address<TAccountMarketVault>;
  systemProgram?: Address<TAccountSystemProgram>;
  marketId: RefundMarketInstructionDataArgs["marketId"];
  refunds: RefundMarketInstructionDataArgs["refunds"];
};

export async function getRefundMarketInstructionAsync<
  TAccountAdmin extends string,
  TAccountMarketVault extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof VAULT_PROGRAM_ADDRESS,
>(
  input: RefundMarketAsyncInput<
    TAccountAdmin,
    TAccountMarketVault,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  RefundMarketInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountMarketVault,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? VAULT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    admin: { value: input.admin ?? null, isWritable: true },
    marketVault: { value: input.marketVault ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.marketVault.value) {
    accounts.marketVault.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(new Uint8Array([109, 97, 114, 107, 101, 116])),
        getUtf8Encoder().encode(expectSome(args.marketId)),
      ],
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.admin),
      getAccountMeta(accounts.marketVault),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getRefundMarketInstructionDataEncoder().encode(
      args as RefundMarketInstructionDataArgs,
    ),
    programAddress,
  } as RefundMarketInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountMarketVault,
    TAccountSystemProgram
  >);
}

export type RefundMarketInput<
  TAccountAdmin extends string = string,
  TAccountMarketVault extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  admin: TransactionSigner<TAccountAdmin>;
  marketVault: Address<TAccountMarketVault>;
  systemProgram?: Address<TAccountSystemProgram>;
  marketId: RefundMarketInstructionDataArgs["marketId"];
  refunds: RefundMarketInstructionDataArgs["refunds"];
};

export function getRefundMarketInstruction<
  TAccountAdmin extends string,
  TAccountMarketVault extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof VAULT_PROGRAM_ADDRESS,
>(
  input: RefundMarketInput<
    TAccountAdmin,
    TAccountMarketVault,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): RefundMarketInstruction<
  TProgramAddress,
  TAccountAdmin,
  TAccountMarketVault,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? VAULT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    admin: { value: input.admin ?? null, isWritable: true },
    marketVault: { value: input.marketVault ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.admin),
      getAccountMeta(accounts.marketVault),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getRefundMarketInstructionDataEncoder().encode(
      args as RefundMarketInstructionDataArgs,
    ),
    programAddress,
  } as RefundMarketInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountMarketVault,
    TAccountSystemProgram
  >);
}

export type ParsedRefundMarketInstruction<
  TProgram extends string = typeof VAULT_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    admin: TAccountMetas[0];
    marketVault: TAccountMetas[1];
    systemProgram: TAccountMetas[2];
  };
  data: RefundMarketInstructionData;
};

export function parseRefundMarketInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedRefundMarketInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      admin: getNextAccount(),
      marketVault: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getRefundMarketInstructionDataDecoder().decode(instruction.data),
  };
}
