/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getArrayDecoder,
  getArrayEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import { VAULT_PROGRAM_ADDRESS } from "../programs";
import {
  expectSome,
  getAccountMetaFactory,
  type ResolvedAccount,
} from "../shared";
import {
  getPayoutDecoder,
  getPayoutEncoder,
  type Payout,
  type PayoutArgs,
} from "../types";

export const DISTRIBUTE_PAYOUTS_DISCRIMINATOR = new Uint8Array([
  226, 196, 33, 224, 8, 117, 184, 243,
]);

export function getDistributePayoutsDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    DISTRIBUTE_PAYOUTS_DISCRIMINATOR,
  );
}

export type DistributePayoutsInstruction<
  TProgram extends string = typeof VAULT_PROGRAM_ADDRESS,
  TAccountAdmin extends string | AccountMeta<string> = string,
  TAccountMarketVault extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAdmin extends string
        ? WritableSignerAccount<TAccountAdmin> &
            AccountSignerMeta<TAccountAdmin>
        : TAccountAdmin,
      TAccountMarketVault extends string
        ? WritableAccount<TAccountMarketVault>
        : TAccountMarketVault,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type DistributePayoutsInstructionData = {
  discriminator: ReadonlyUint8Array;
  marketId: string;
  payouts: Array<Payout>;
};

export type DistributePayoutsInstructionDataArgs = {
  marketId: string;
  payouts: Array<PayoutArgs>;
};

export function getDistributePayoutsInstructionDataEncoder(): Encoder<DistributePayoutsInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["marketId", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
      ["payouts", getArrayEncoder(getPayoutEncoder())],
    ]),
    (value) => ({ ...value, discriminator: DISTRIBUTE_PAYOUTS_DISCRIMINATOR }),
  );
}

export function getDistributePayoutsInstructionDataDecoder(): Decoder<DistributePayoutsInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["marketId", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ["payouts", getArrayDecoder(getPayoutDecoder())],
  ]);
}

export function getDistributePayoutsInstructionDataCodec(): Codec<
  DistributePayoutsInstructionDataArgs,
  DistributePayoutsInstructionData
> {
  return combineCodec(
    getDistributePayoutsInstructionDataEncoder(),
    getDistributePayoutsInstructionDataDecoder(),
  );
}

export type DistributePayoutsAsyncInput<
  TAccountAdmin extends string = string,
  TAccountMarketVault extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  admin: TransactionSigner<TAccountAdmin>;
  marketVault?: Address<TAccountMarketVault>;
  systemProgram?: Address<TAccountSystemProgram>;
  marketId: DistributePayoutsInstructionDataArgs["marketId"];
  payouts: DistributePayoutsInstructionDataArgs["payouts"];
};

export async function getDistributePayoutsInstructionAsync<
  TAccountAdmin extends string,
  TAccountMarketVault extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof VAULT_PROGRAM_ADDRESS,
>(
  input: DistributePayoutsAsyncInput<
    TAccountAdmin,
    TAccountMarketVault,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  DistributePayoutsInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountMarketVault,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? VAULT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    admin: { value: input.admin ?? null, isWritable: true },
    marketVault: { value: input.marketVault ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.marketVault.value) {
    accounts.marketVault.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(new Uint8Array([109, 97, 114, 107, 101, 116])),
        getUtf8Encoder().encode(expectSome(args.marketId)),
      ],
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.admin),
      getAccountMeta(accounts.marketVault),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getDistributePayoutsInstructionDataEncoder().encode(
      args as DistributePayoutsInstructionDataArgs,
    ),
    programAddress,
  } as DistributePayoutsInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountMarketVault,
    TAccountSystemProgram
  >);
}

export type DistributePayoutsInput<
  TAccountAdmin extends string = string,
  TAccountMarketVault extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  admin: TransactionSigner<TAccountAdmin>;
  marketVault: Address<TAccountMarketVault>;
  systemProgram?: Address<TAccountSystemProgram>;
  marketId: DistributePayoutsInstructionDataArgs["marketId"];
  payouts: DistributePayoutsInstructionDataArgs["payouts"];
};

export function getDistributePayoutsInstruction<
  TAccountAdmin extends string,
  TAccountMarketVault extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof VAULT_PROGRAM_ADDRESS,
>(
  input: DistributePayoutsInput<
    TAccountAdmin,
    TAccountMarketVault,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): DistributePayoutsInstruction<
  TProgramAddress,
  TAccountAdmin,
  TAccountMarketVault,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? VAULT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    admin: { value: input.admin ?? null, isWritable: true },
    marketVault: { value: input.marketVault ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.admin),
      getAccountMeta(accounts.marketVault),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getDistributePayoutsInstructionDataEncoder().encode(
      args as DistributePayoutsInstructionDataArgs,
    ),
    programAddress,
  } as DistributePayoutsInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountMarketVault,
    TAccountSystemProgram
  >);
}

export type ParsedDistributePayoutsInstruction<
  TProgram extends string = typeof VAULT_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    admin: TAccountMetas[0];
    marketVault: TAccountMetas[1];
    systemProgram: TAccountMetas[2];
  };
  data: DistributePayoutsInstructionData;
};

export function parseDistributePayoutsInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedDistributePayoutsInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      admin: getNextAccount(),
      marketVault: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getDistributePayoutsInstructionDataDecoder().decode(instruction.data),
  };
}
